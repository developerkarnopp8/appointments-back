'use strict';

/**
 * nome-convidado controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::nome-convidado.nome-convidado');
